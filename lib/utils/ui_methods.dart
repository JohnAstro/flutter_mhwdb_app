import 'package:flutter/material.dart';

roundedBox() {
  return BoxDecoration(
    border: Border.all(color: const Color(0xFF70647d), width: 2.0),
    borderRadius: BorderRadius.circular(10),
  );
}
